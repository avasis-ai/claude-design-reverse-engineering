# Overview

This repository captures **community-maintained** notes on how long-form assistant-style answers tend to be structured for human readers, and maps those habits to **reusable web tokens** and checklists.

Start at `output-design-framework.md`, then `CHECKLIST.md`. Tokens live in `packages/tokens/tokens.css`.

For attribution practices, read `research-attribution.md`.
